{
  "name": "slidestage-pro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SlideStage Pro: self-hosted .stage platform built on Prisma + SQLite + Hono + React.",
  "packageManager": "pnpm@11.1.3",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "predev": "pnpm -r --filter \"./packages/*\" build",
    "dev": "pnpm --parallel --filter \"./apps/*\" dev",
    "build": "pnpm -r --filter \"./packages/*\" build && pnpm -r --filter \"./apps/*\" build",
    "pretypecheck": "pnpm -r --filter \"./packages/*\" build",
    "typecheck": "pnpm -r typecheck",
    "pretest": "pnpm -r --filter \"./packages/*\" build",
    "test": "pnpm -r test",
    "check": "pnpm check:boundaries && pnpm typecheck && pnpm test && pnpm build",
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "check:boundaries:self-test": "node scripts/check-boundaries.test.mjs",
    "fixtures": "node scripts/build-fixtures.mjs",
    "fixtures:check": "node scripts/build-fixtures.mjs --check",
    "db:generate": "pnpm --filter @slidestage/pro-api db:generate",
    "db:migrate": "pnpm --filter @slidestage/pro-api db:migrate",
    "db:migrate:dev": "pnpm --filter @slidestage/pro-api db:migrate:dev",
    "db:seed": "pnpm --filter @slidestage/pro-api db:seed",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "fflate": "^0.8.2"
  }
}
